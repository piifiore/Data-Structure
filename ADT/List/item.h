typedef int item;
#define NULLITEM 0

// Funzione per verificare l'uguaglianza tra due elementi
int eq(item x, item y);
// Funzione per acquisire un elemento da input
void input_item(item *x);
// Funzione per stampare un elemento
void output_item(item x);